[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Tell Netlify's secret scanner to ignore these public Next.js variables
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_ANON_KEY,NEXT_PUBLIC_SUPABASE_URL"
